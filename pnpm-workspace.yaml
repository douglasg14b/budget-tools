packages:
    - apps/**
    - packages/**

# argon2 ships prebuilt binaries but still declares an install script; pnpm blocks build
# scripts by default. Allow it so password hashing works on a fresh clone without a manual
# `pnpm approve-builds` step.
onlyBuiltDependencies:
    - argon2
